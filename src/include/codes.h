#ifndef DROP_CODES_H
#define DROP_CODES_H

#include <stdlib.h>

enum
{
    NOOP = 0,
    HALT,

    FUNC,
    END,

    PUSH, PUSH16, PUSH32, PUSH64,
    POP, POP16, POP32, POP64,
    LOAD, LOAD16, LOAD32, LOAD64,
    MOVE, MOVE16, MOVE32, MOVE64,
    CREATE_DSTACK, DELETE_DSTACK,

    ADD, ADD16, ADD32, ADD64,
    SUB, SUB16, SUB32, SUB64,
    MULT, MULT16, MULT32, MULT64,
    DIV, DIV16, DIV32, DIV64,

    CALL,
    RETURN,

    JUMP,
    IFE,
    IFNE,
    IFGT,
    IFLT,

    PRINT,
    PRINTC,
    PRINTI,
};

#endif